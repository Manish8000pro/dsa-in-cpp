#include<iostream>
#include<set>
using namespace std;

int main(){
    // set<int>s;
    // s.insert(10);
    // s.insert(20);
    // s.insert(30);
    // s.insert(40);
    // s.insert(50);

    // for(auto x: s){
    //     cout<<x<<" ";
    // }

    // set<pair<int,int>>s1;
    // s1.insert({10,20});
    // s1.insert({5,11});
    // s1.insert({54,8});
    // s1.insert({7,9});
    // s1.insert({10,11});

    // for(auto x: s1){
    //     cout<<x.first<<" "<<x.second<<endl;
    // }

    // cout<<s1.size();
    // s1.erase({10,11});
    // cout<<s1.size();

    set<int>s;
    s.insert(10);
    s.insert(20);
    s.insert(30);

    // auto it = s.find(90);
    // cout<<*it<<endl;

    return 0;
}